TODO: for a member expr (and possibly others), if it's not computed, the key could be an ident OR private name. I have often assumed it can only
be an ident, which is wrong.

ControlFlowGraph:
class Foo111 {
  a=(console.log(1),1);
  static [(console.log(2),1)]=(console.log(3),1);
}

Infinity is an ident...

Switch creates scope e.g. for rename vars

https://hardekbc.github.io/files/dewey15parallel.pdf