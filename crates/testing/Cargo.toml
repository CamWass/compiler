[package]
edition = "2024"
name = "testing"
version = "0.1.0"

[dependencies]
difference = { workspace = true }
pretty_assertions = { workspace = true }

global_common = { path = "../common", features = ["tty-emitter"] }
