[package]
edition = "2024"
license = "Apache-2.0/MIT"
name = "testing"
version = "0.1.0"

[dependencies]
difference = { workspace = true }
pretty_assertions = { workspace = true }

global_common = { path = "../crates/common", features = ["tty-emitter"] }
